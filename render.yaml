
services:
  - type: web
    name: AnnieXMusic
    plan: free
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m ANNIEMUSIC
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /
    healthCheckTimeoutSeconds: 15  # Increased timeout
